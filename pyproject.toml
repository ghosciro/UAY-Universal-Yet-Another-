[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "uay"
version = "0.2.0"
dependencies = [
    "rapidfuzz"
]

[project.scripts]
uay = "cli:main"

[tool.setuptools]
py-modules = ["cli", "engine", "models", "ranker", "ui"]
packages = ["backends"]