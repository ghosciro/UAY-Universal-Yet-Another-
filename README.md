# Uay-py

Un wrapper Python per package manager ispirato a `yay`, con supporto per `apt` e `flatpak`.
Utilizza `fzf` per la selezione interattiva dei pacchetti e `rapidfuzz` per il ranking dei risultati.
